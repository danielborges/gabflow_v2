use_default_settings: true

general:
  debug: false
  instance_name: "GabFlow Pesquisa"

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: "overridden-by-SEARXNG_SECRET"
  limiter: false
  image_proxy: false
